services:
  api:
    ports: !reset []

  frontend:
    build:
      args:
        VITE_API_URL: /api/v1
    ports: !override
      - "3001:80"
    depends_on:
      - api

volumes:
  postgres_data:
  uploads:
