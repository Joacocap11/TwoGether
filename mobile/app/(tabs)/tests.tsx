import React, { useState } from 'react';
import { FlatList, Pressable, Text, TextInput, View } from 'react-native';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { useRouter } from 'expo-router';
import { api, TestRecord } from '../../src/api';
import { Button, ConfirmDelete, DateText, ErrorState, Field, Loading, Photo, styles, colors } from '../../src/ui';
export default function Tests() { const query = useQuery({ queryKey: ['tests'], queryFn: api.tests }); const router = useRouter(); if (query.isPending) return <Loading />; if (query.isError) return <ErrorState message={(query.error as Error).message} retry={() => query.refetch()} />; return <FlatList data={query.data} keyExtractor={x => String(x.id)} style={styles.screen} contentContainerStyle={styles.content} refreshing={query.isRefetching} onRefresh={() => query.refetch()} ListHeaderComponent={<><Text style={styles.title}>Tests</Text><Text style={styles.subtitle}>Two captures for every result.</Text><Button title="＋ New test" onPress={() => router.push('/test/new')} /></>} renderItem={({ item }) => <Pressable style={[styles.card, { flexDirection: 'row', gap: 13 }]} onPress={() => router.push(`/test/${item.id}`)}><Photo path={item.image_path} /><View><Text style={{ color: colors.ink, fontSize: 18, fontWeight: '800' }}>{item.title}</Text><DateText value={item.test_date} /><Text style={styles.muted}>{item.outcomes?.length ?? 0} captures</Text></View></Pressable>} ListEmptyComponent={<View style={styles.empty}><Text style={styles.muted}>No tests yet.</Text></View>} />; }
export function TestForm({ item, onDone }: { item?: TestRecord; onDone: (value: TestRecord) => void }) { const router = useRouter(); const qc = useQueryClient(); const users = useQuery({ queryKey: ['users'], queryFn: api.users }); const [title, setTitle] = useState(item?.title ?? ''); const [date, setDate] = useState(item?.test_date ?? ''); const [notes, setNotes] = useState(item?.notes ?? ''); const [error, setError] = useState(''); const mutation = useMutation({ mutationFn: () => { const ids = (users.data ?? []).slice(0, 2).map(x => x.id); if (!title || !/^\d{4}-\d{2}-\d{2}$/.test(date) || ids.length !== 2) throw new Error('Add a title, ISO date (YYYY-MM-DD), and two active users.'); return item ? api.updateTest(item.id, { title, test_date: date, notes: notes || null, outcomes: ids.map(user_id => ({ user_id })) }) : api.createTest({ title, test_date: date, notes: notes || null, outcomes: ids.map(user_id => ({ user_id })) }); }, onSuccess: value => { qc.invalidateQueries({ queryKey: ['tests'] }); onDone(value); router.replace(`/test/${value.id}`); }, onError: e => setError(e instanceof Error ? e.message : 'Unable to save') }); return <View>{error ? <Text style={styles.error}>{error}</Text> : null}<Field label="Title" value={title} onChangeText={setTitle} /><Field label="Test date (YYYY-MM-DD)" value={date} onChangeText={setDate} /><Field label="Notes" value={notes} onChangeText={setNotes} multiline /><Button title={mutation.isPending ? 'Saving…' : 'Save test'} onPress={() => mutation.mutate()} disabled={mutation.isPending} /></View>; }
