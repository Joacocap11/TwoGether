import React, { useState } from 'react';
import { Image, Modal, Pressable, ScrollView, Text, View } from 'react-native';
import { useLocalSearchParams, useRouter } from 'expo-router';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { api, imageUrl } from '../../src/api';
import type { MediaRating } from '../../src/api';
import { pickImage } from '../../src/picker';
import { Button, ConfirmDelete, DateText, ErrorState, Field, Loading, Photo, RatingEditor, styles } from '../../src/ui';
export default function MediaDetail() { const { id } = useLocalSearchParams<{ id: string }>(); const isNew = id === 'new'; const numeric = Number(id); const query = useQuery({ queryKey: ['mediaOne', numeric], queryFn: () => api.mediaOne(numeric), enabled: !isNew }); if (!isNew && query.isPending) return <Loading />; if (!isNew && query.isError) return <ErrorState message={(query.error as Error).message} retry={() => query.refetch()} />; return <MediaEditor item={query.data} id={numeric} isNew={isNew} />; }
function MediaEditor({ item, id, isNew }: { item?: import('../../src/api').Media; id: number; isNew: boolean }) { const router = useRouter(); const qc = useQueryClient();  const users = useQuery({ queryKey: ['users'], queryFn: api.users }); const [title, setTitle] = useState(item?.title ?? ''); const [date, setDate] = useState(item?.watched_date ?? ''); const [category, setCategory] = useState(item?.category ?? ''); const [type, setType] = useState<'movie' | 'series'>(item?.media_type ?? 'movie'); const [ratings, setRatings] = useState<MediaRating[]>(item?.ratings?.length === 2 ? item.ratings : (users.data ?? []).slice(0, 2).map(x => ({ user_id: x.id, score: 8, opinion: '' }))); const [error, setError] = useState(''); const [poster, setPoster] = useState(item?.image_path); const [modal, setModal] = useState(false); const save = useMutation({ mutationFn: () => { if (!title || !/^\d{4}-\d{2}-\d{2}$/.test(date) || ratings.length !== 2) throw new Error('Agrega un título, una fecha ISO (AAAA-MM-DD) y dos valoraciones.'); return isNew ? api.createMedia({ title, media_type: type, watched_date: date, category: category || null, ratings }) : api.updateMedia(id, { title, media_type: type, watched_date: date, category: category || null, ratings }); }, onSuccess: value => { qc.invalidateQueries({ queryKey: ['media'] }); qc.setQueryData(['mediaOne', value.id], value); router.replace(`/media/${value.id}`); }, onError: e => setError(e instanceof Error ? e.message : 'No se pudo guardar') }); const upload = async () => { const file = await pickImage(); if (!file || isNew) return; const result = await api.uploadMedia(id, file); setPoster(result.image_path); qc.invalidateQueries({ queryKey: ['media'] }); }; return <ScrollView style={styles.screen} contentContainerStyle={styles.content}>{!isNew ? <View style={{ flexDirection: 'row', justifyContent: 'space-between' }}><View><Text style={styles.title}>{title}</Text><DateText value={date} /></View><ConfirmDelete label="serie o película" onConfirm={async () => { await api.deleteMedia(id); qc.invalidateQueries({ queryKey: ['media'] }); router.back(); }} /></View> : <Text style={styles.title}>Nueva serie o película</Text>}<Field label="Título" value={title} onChangeText={setTitle} /><Field label="Fecha (AAAA-MM-DD)" value={date} onChangeText={setDate} /><Field label="Categoría" value={category} onChangeText={setCategory} /><View style={{ flexDirection: 'row', marginBottom: 13 }}>{(['movie', 'series'] as const).map(value => <Pressable key={value} style={[styles.chip, type === value && styles.chipActive]} onPress={() => setType(value)}><Text style={[styles.chipText, type === value && styles.chipTextActive]}>{value === 'movie' ? 'Película' : 'Serie'}</Text></Pressable>)}</View>{ratings.map((rating, index) => <RatingEditor key={rating.user_id} name={users.data?.find(x => x.id === rating.user_id)?.name ?? `Persona ${index + 1}`} score={rating.score} opinion={rating.opinion ?? ''} onScore={score => setRatings(current => current.map((x, i) => i === index ? { ...x, score } : x))} onOpinion={opinion => setRatings(current => current.map((x, i) => i === index ? { ...x, opinion } : x))} />)}{error ? <Text style={styles.error}>{error}</Text> : null}<Button title={save.isPending ? 'Guardando…' : 'Guardar'} onPress={() => save.mutate()} disabled={save.isPending} />{!isNew ? <><Pressable onPress={() => poster && setModal(true)} style={{ alignSelf: 'center', marginTop: 18 }}><Photo path={poster} size={180} /></Pressable><Button title="Subir póster" onPress={upload} secondary /></> : null}<Modal visible={modal} transparent onRequestClose={() => setModal(false)}><Pressable style={{ flex: 1, backgroundColor: '#000c', justifyContent: 'center' }} onPress={() => setModal(false)}><Image source={{ uri: poster ? imageUrl(poster) : undefined }} style={{ width: '100%', height: '80%', resizeMode: 'contain' }} /></Pressable></Modal></ScrollView>; }
