import React,{useEffect,useState} from 'react';
import {Check,Plus,Pencil,Trash2,X} from 'lucide-react';
import {api,endpoints,imageUrl,MediaEntry,MediaType,HotelVisit,User} from './api';
import {ImageLightbox,Photo,Score} from './components';
const label=(type:MediaType)=>type==='series'?'Serie':'Película';
const people=(users:User[])=>[users.find(u=>/joa?co|joaqu[ií]n/i.test(u.name))||users[0],users.find(u=>/selena/i.test(u.name))||users[1]].filter(Boolean) as User[];
function MediaForm({item,users,onCancel,onDone}:{item?:MediaEntry;users:User[];onCancel:()=>void;onDone:()=>void}){const ps=people(users),[title,setTitle]=useState(item?.title||''),[type,setType]=useState<MediaType>(item?.media_type||'movie'),[date,setDate]=useState(item?.watched_date||''),[category,setCategory]=useState(item?.category||''),[joaco,setJoaco]=useState<number|string>(item?.ratings.find(r=>r.user_id===ps[0]?.id)?.score||''),[selena,setSelena]=useState<number|string>(item?.ratings.find(r=>r.user_id===ps[1]?.id)?.score||''),[file,setFile]=useState<File>(),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);try{const body={title,media_type:type,watched_date:date,category:category||null,ratings:[{user_id:ps[0].id,score:Number(joaco)},{user_id:ps[1].id,score:Number(selena)}]};const saved=await api<MediaEntry>(item?endpoints.mediaItem(item.id):endpoints.media,{method:item?'PUT':'POST',body:JSON.stringify(body)});if(file){const form=new FormData();form.append('image',file);await api(endpoints.mediaUpload(saved.id),{method:'POST',body:form})}onDone()}catch(x){setError((x as Error).message)}finally{setBusy(false)}}return <div className="modal"><form className="form" onSubmit={submit}><div className="form-head"><h2>{item?'Editar':'Nueva'} serie / película</h2><button type="button" className="icon-btn" onClick={onCancel} aria-label="Cerrar"><X/></button></div><label>Título<input required value={title} onChange={e=>setTitle(e.target.value)}/></label><fieldset className="category-field"><legend>Tipo</legend><div className="category-options">{(['series','movie'] as MediaType[]).map(v=><button type="button" key={v} className={type===v?'selected':''} aria-pressed={type===v} onClick={()=>setType(v)}>{label(v)}</button>)}</div></fieldset><label>Fecha<input required type="date" value={date} onChange={e=>setDate(e.target.value)}/></label><label>Categoría / género<input value={category} onChange={e=>setCategory(e.target.value)} placeholder="Terror, comedia…"/></label><Photo id={`media-${item?.id||'new'}-image`} file={file} existing={item?.image_path} onChange={setFile} label="Poster o portada"/><div className="media-ratings"><Score person={ps[0].name} value={joaco} onChange={setJoaco}/><Score person={ps[1].name} value={selena} onChange={setSelena}/></div><p className="error">{error}</p><div className="form-actions"><button type="button" onClick={onCancel}>Cancelar</button><button className="primary" disabled={busy}>{busy?<span>Guardando…</span>:<><Check size={17}/> Guardar</>}</button></div></form></div>}
function MediaCard({item,onOpen,onEdit,onDelete,users}:{item:MediaEntry;onOpen:()=>void;onEdit:()=>void;onDelete:()=>void;users:User[]}){const ps=people(users),scores=ps.map(u=>item.ratings.find(r=>r.user_id===u.id)?.score||0),avg=item.average_rating||scores.reduce((a,b)=>a+b,0)/scores.filter(Boolean).length;return <article className="media-card" onClick={onOpen}><div className="media-cover">{item.image_path?<img src={imageUrl(item.image_path)} alt={`Poster de ${item.title}`}/>:<span>Sin poster</span>}</div><div className="body"><div className="card-heading"><h3>{item.title}</h3><span className="category-badge">{label(item.media_type)}</span></div>{item.category&&<p className="media-category">{item.category}</p>}<p>{item.watched_date}</p><div className="media-score"><span>Joaco <b>{scores[0]||'–'}</b></span><span>Selena <b>{scores[1]||'–'}</b></span><span>Promedio <b>{avg?avg.toFixed(1):'–'}</b></span></div><div className="card-actions"><button type="button" onClick={e=>{e.stopPropagation();onEdit()}} aria-label="Editar"><Pencil size={15}/></button><button type="button" onClick={e=>{e.stopPropagation();onDelete()}} aria-label="Eliminar"><Trash2 size={15}/></button></div></div></article>}
export function MediaPage({users}:{users:User[]}){const[items,setItems]=useState<MediaEntry[]>([]),[filter,setFilter]=useState<MediaType>(),[query,setQuery]=useState(''),[editing,setEditing]=useState<MediaEntry>(),[detail,setDetail]=useState<MediaEntry>(),[show,setShow]=useState(false),[lightbox,setLightbox]=useState<string>();const load=async()=>setItems(await api<MediaEntry[]>(endpoints.media));useEffect(()=>{load()},[]);const filtered=items.filter(x=>(!filter||x.media_type===filter)&&x.title.toLowerCase().includes(query.toLowerCase()));async function remove(item:MediaEntry){if(confirm('¿Eliminar este registro?')){await api(endpoints.mediaItem(item.id),{method:'DELETE'});load()}}if(detail)return <><button className="back" onClick={()=>setDetail(undefined)}>Todas las series / películas</button><div className="detail-header"><div><p className="eyebrow">SERIES / PELÍCULAS</p><h1>{detail.title}</h1><p className="muted">{label(detail.media_type)} · {detail.watched_date}</p></div><button onClick={()=>{setEditing(detail);setShow(true)}}><Pencil size={16}/> Editar</button></div>{detail.category&&<p className="notes">{detail.category}</p>}{detail.image_path&&<button className="detail-general-photo" onClick={()=>setLightbox(detail.image_path!)}><img src={imageUrl(detail.image_path)} alt={`Poster de ${detail.title}`}/></button>}<div className="media-detail-scores">{people(users).map(u=><Score key={u.id} person={u.name} value={detail.ratings.find(r=>r.user_id===u.id)?.score||''} onChange={()=>{}}/>)}</div><ImageLightbox src={lightbox} alt={detail.title} onClose={()=>setLightbox(undefined)}/>{show&&<MediaForm item={editing} users={users} onCancel={()=>setShow(false)} onDone={()=>{setShow(false);setDetail(undefined);load()}}/>}</>;return <><div className="title"><div><p className="eyebrow">LO QUE VIMOS JUNTOS</p><h1>Series / Películas</h1></div><button className="primary" onClick={()=>{setEditing(undefined);setShow(true)}}><Plus size={17}/> Nueva serie / película</button></div><div className="search"><input aria-label="Buscar series o películas" placeholder="Buscar por título…" value={query} onChange={e=>setQuery(e.target.value)}/></div><div className="category-filters"><button className={!filter?'selected':''} onClick={()=>setFilter(undefined)}>Todas</button><button className={filter==='series'?'selected':''} onClick={()=>setFilter('series')}>Series</button><button className={filter==='movie'?'selected':''} onClick={()=>setFilter('movie')}>Películas</button></div><div className="media-grid">{filtered.map(x=><MediaCard key={x.id} item={x} users={users} onOpen={()=>setDetail(x)} onEdit={()=>{setEditing(x);setShow(true)}} onDelete={()=>remove(x)}/>)}</div>{show&&<MediaForm item={editing} users={users} onCancel={()=>setShow(false)} onDone={()=>{setShow(false);load()}}/>}</>}
function HotelForm({item,users,onCancel,onDone}:{item?:HotelVisit;users:User[];onCancel:()=>void;onDone:()=>void}){const ps=people(users),[name,setName]=useState(item?.name||''),[date,setDate]=useState(item?.visit_date||''),[location,setLocation]=useState(item?.location||''),[joaco,setJoaco]=useState<number|string>(item?.ratings.find(r=>r.user_id===ps[0]?.id)?.score||''),[selena,setSelena]=useState<number|string>(item?.ratings.find(r=>r.user_id===ps[1]?.id)?.score||''),[joacoOpinion,setJoacoOpinion]=useState(item?.ratings.find(r=>r.user_id===ps[0]?.id)?.opinion||''),[selenaOpinion,setSelenaOpinion]=useState(item?.ratings.find(r=>r.user_id===ps[1]?.id)?.opinion||''),[file,setFile]=useState<File>(),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);try{const body={name,visit_date:date,location:location||null,ratings:[{user_id:ps[0].id,score:Number(joaco),opinion:joacoOpinion||null},{user_id:ps[1].id,score:Number(selena),opinion:selenaOpinion||null}]};const saved=await api<HotelVisit>(item?endpoints.hotel(item.id):endpoints.hotels,{method:item?'PUT':'POST',body:JSON.stringify(body)});if(file){const form=new FormData();form.append('image',file);await api(endpoints.hotelUpload(saved.id),{method:'POST',body:form})}onDone()}catch(x){setError((x as Error).message)}finally{setBusy(false)}}return <div className="modal"><form className="form" onSubmit={submit}><div className="form-head"><h2>{item?'Editar':'Nuevo'} hotel</h2><button type="button" className="icon-btn" onClick={onCancel} aria-label="Cerrar"><X/></button></div><label>Nombre del hotel<input required value={name} onChange={e=>setName(e.target.value)}/></label><label>Fecha<input required type="date" value={date} onChange={e=>setDate(e.target.value)}/></label><label>Ubicación<input value={location} onChange={e=>setLocation(e.target.value)}/></label><Photo id={`hotel-${item?.id||'new'}-image`} file={file} existing={item?.image_path} onChange={setFile} label="Foto del hotel"/><label>Opinión de Joaco<textarea value={joacoOpinion} onChange={e=>setJoacoOpinion(e.target.value)}/></label><Score person={ps[0].name} value={joaco} onChange={setJoaco}/><label>Opinión de Selena<textarea value={selenaOpinion} onChange={e=>setSelenaOpinion(e.target.value)}/></label><Score person={ps[1].name} value={selena} onChange={setSelena}/><p className="error">{error}</p><div className="form-actions"><button type="button" onClick={onCancel}>Cancelar</button><button className="primary" disabled={busy}>{busy?'Guardando…':'Guardar hotel'}</button></div></form></div>}
export function HotelPage({users}:{users:User[]}){const[items,setItems]=useState<HotelVisit[]>([]),[editing,setEditing]=useState<HotelVisit>(),[detail,setDetail]=useState<HotelVisit>(),[show,setShow]=useState(false),[lightbox,setLightbox]=useState<string>();const load=async()=>setItems(await api<HotelVisit[]>(endpoints.hotels));useEffect(()=>{load()},[]);async function remove(item:HotelVisit){if(confirm('¿Eliminar este hotel?')){await api(endpoints.hotel(item.id),{method:'DELETE'});load()}}if(detail)return <><button className="back" onClick={()=>setDetail(undefined)}>Todos los hoteles</button><div className="detail-header"><div><p className="eyebrow">HOTEL</p><h1>{detail.name}</h1><p className="muted">{detail.visit_date} · {detail.location||'Sin ubicación'}</p></div><button onClick={()=>{setEditing(detail);setShow(true)}}><Pencil size={16}/> Editar</button></div>{detail.image_path&&<button className="detail-general-photo" onClick={()=>setLightbox(detail.image_path!)}><img src={imageUrl(detail.image_path)} alt={`Foto de ${detail.name}`}/></button>}{people(users).map(u=>{const r=detail.ratings.find(x=>x.user_id===u.id);return <article className="opinion" key={u.id}><h3>{u.name}</h3><Score person={u.name} value={r?.score||''} onChange={()=>{}}/>{r?.opinion&&<p>{r.opinion}</p>}</article>})}<ImageLightbox src={lightbox} alt={detail.name} onClose={()=>setLightbox(undefined)}/>{show&&<HotelForm item={editing} users={users} onCancel={()=>setShow(false)} onDone={()=>{setShow(false);setDetail(undefined);load()}}/>}</>;return <><div className="title"><div><p className="eyebrow">ESCAPADAS</p><h1>Hoteles</h1></div><button className="primary" onClick={()=>{setEditing(undefined);setShow(true)}}><Plus size={17}/> Nuevo hotel</button></div><div className="hotel-grid">{items.map(x=><article className="hotel-card" key={x.id} onClick={()=>setDetail(x)}><div className="hotel-cover">{x.image_path?<img src={imageUrl(x.image_path)} alt={`Foto de ${x.name}`}/>:<span>Sin foto</span>}</div><div className="body"><h3>{x.name}</h3><p>{x.location||'Sin ubicación'} · {x.visit_date}</p><div className="media-score">{people(users).map(u=><span key={u.id}>{u.name} <b>{x.ratings.find(r=>r.user_id===u.id)?.score||'–'}</b></span>)}<span>Promedio <b>{x.average_rating?.toFixed(1)||'–'}</b></span></div><div className="card-actions"><button type="button" onClick={e=>{e.stopPropagation();setEditing(x);setShow(true)}} aria-label="Editar"><Pencil size={15}/></button><button type="button" onClick={e=>{e.stopPropagation();remove(x)}} aria-label="Eliminar"><Trash2 size={15}/></button></div></div></article>)}</div>{show&&<HotelForm item={editing} users={users} onCancel={()=>setShow(false)} onDone={()=>{setShow(false);load()}}/>}</>}
